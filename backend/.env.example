# Production: set these in your platform (e.g. Heroku, Railway); .env file is optional.
# Backend does not load .env when NODE_ENV=production.

PORT=5500
MONGODB_URI=mongodb://localhost:27017/smartbiz
# Or use MONGO_URI for compatibility
JWT_SECRET=your_jwt_secret_here
OPENAI_API_KEY=your_openai_key_here
# Required in production for CORS
FRONTEND_URL=https://your-frontend-domain.com
